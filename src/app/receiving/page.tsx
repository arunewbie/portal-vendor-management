import AppShell from "@/components/AppShell";
import { prisma } from "@/lib/prisma";
import { StatusBadge } from "@/components/StatusBadge";
export default async function ReceivingPage() {
  const orders = await prisma.order.findMany({ where: { status: { notIn: ["CLOSED", "FORCE_CLOSED"] }}, include: { supplier: true, receivings: true }, orderBy: { deliveryDate: "asc" }});
  return <AppShell><h1 className="mb-2 text-2xl font-bold">Receiving</h1><p className="mb-6 text-sm text-slate-500">Konfirmasi actual barang datang, partial, full, atau force close.</p><div className="card overflow-x-auto"><table className="w-full"><thead><tr><th className="table-th">Supplier</th><th className="table-th">Part</th><th className="table-th">Order Qty</th><th className="table-th">Received</th><th className="table-th">Balance</th><th className="table-th">Status</th><th className="table-th">Input</th></tr></thead><tbody>{orders.map(o=>{const actual=o.receivings.reduce((a,b)=>a+b.receivedQty,0); return <tr key={o.id} className="border-t"><td className="table-td">{o.supplier.name}</td><td className="table-td">{o.partNo}</td><td className="table-td">{o.qtyOrder}</td><td className="table-td">{actual}</td><td className="table-td font-semibold">{Math.max(o.qtyOrder-actual,0)}</td><td className="table-td"><StatusBadge status={o.status}/></td><td className="table-td"><form action="/api/receiving" method="post" className="flex gap-2"><input type="hidden" name="orderId" value={o.id}/><input className="input w-28" name="receivedQty" type="number" placeholder="Qty"/><button className="btn-primary">Receive</button><button name="forceClose" value="1" className="btn-secondary">Force Close</button></form></td></tr>})}</tbody></table></div></AppShell>
}
